<h1>id: <%= @task.id %> の編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '変更' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>